/** @type {import('./$types').PageLoad} */

export function load({ params }) {

}

// 1. create load function to pull individual user data
    // this might be a problem due to needing to access localstorage
// 2. make data available at page.js